# 🏛️ The Historical Court (Multi-Agent System)

โปรเจกต์พัฒนาระบบ Multi-Agent ด้วย **Google ADK** เพื่อวิเคราะห์บุคคลหรือเหตุการณ์ทางประวัติศาสตร์ในรูปแบบ **"ศาลจำลอง"** โดยเน้นการสืบค้นข้อมูลที่ขัดแย้งกันจาก Wikipedia เพื่อสร้างรายงานที่สมดุลและเป็นกลางที่สุด

---

## 🚀 โครงสร้างระบบ (Architecture)

ระบบถูกออกแบบมาเป็น 4 ขั้นตอนหลัก (Sequential & Parallel Workflow):

1. **The Inquiry (Entry Point):** รับชื่อหัวข้อจากผู้ใช้ (เช่น Napoleon, Cold War) และบันทึกลงใน Session State (`PROMPT`).
2. **The Investigation (Parallel):** แยกการทำงานออกเป็น 2 ฝั่งพร้อมกัน:
* **Agent A (The Admirer):** ค้นหาเฉพาะ "ความสำเร็จ/ด้านบวก/มรดกที่ยิ่งใหญ่"
* **Agent B (The Critic):** ค้นหาเฉพาะ "ข้อผิดพลาด/ความล้มเหลว/ข้อขัดแย้ง"


3. **The Trial & Review (Loop):** * **Agent C (The Judge):** ตรวจสอบข้อมูลใน `pos_data` และ `neg_data`
* หากข้อมูลฝั่งใดฝั่งหนึ่งน้อยเกินไป Judge จะส่ง Feedback กลับไปให้ Researcher ค้นหาใหม่
* เมื่อข้อมูลสมบูรณ์และสมดุลแล้ว Judge จะเรียกใช้ Tool `exit_loop`


4. **The Verdict (Output):** * **Verdict Writer:** นำข้อมูลทั้งหมดมาสรุปเป็นรายงานที่เป็นกลาง
* **FileSystem Tool:** บันทึกรายงานเป็นไฟล์ `.txt` ในโฟลเดอร์ `court_records/`



---

## 🛠️ เทคนิคทางโปรแกรมมิ่ง (Technical Implementation)

* **State Management:** ใช้ `tool_context.state` แยก Key ชัดเจน ได้แก่ `pos_data`, `neg_data`, `PROMPT`, และ `judge_feedback`
* **Prompt Engineering:** * การใช้ **Templating** `{PROMPT?}` เพื่อส่งต่อบริบทระหว่าง Agent
* การทำ **Keyword Expansion** โดยสั่งให้ Researcher เติมคำค้นหา เช่น `... controversy` หรือ `... achievements` อัตโนมัติ


* **Loop Logic:** ควบคุมการวนซ้ำด้วย `LoopAgent` และต้องจบการทำงานผ่านการตัดสินใจของ Agent (Tool `exit_loop`) เท่านั้น
* **Strict Role Adherence:** ออกแบบ Instruction ให้ Agent ยึดถือบทบาทตนเองอย่างเคร่งครัด (เช่น Critic จะไม่หาข้อมูลด้านบวกเด็ดขาด)

---

## 🧪 ผลการทดสอบ (Test Results)

| Case Study | Expected Result | Status |
| --- | --- | --- |
| **Napoleon Bonaparte** | แยกบทวิเคราะห์กฎหมาย (บวก) และสงคราม (ลบ) ได้ชัดเจน | ✅ Pass |
| **The Cold War** | พบข้อมูล Space Race (บวก) และ Proxy Wars (ลบ) | ✅ Pass |
| **Loop Test** | Judge สั่งค้นหาเพิ่มเมื่อข้อมูลไม่สมดุลในรอบแรก | ✅ Pass |
| **File Creation** | สร้างไฟล์ชื่อ `Napoleon_Bonaparte.txt` ได้ถูกต้อง | ✅ Pass |

---

## 📦 วิธีการติดตั้งและรัน (Setup)

1. ติดตั้ง Dependencies:
```bash
pip install google-adk python-dotenv langchain-community wikipedia

```


2. สร้างไฟล์ `.env` และใส่ API Key:
```env
GOOGLE_API_KEY=your_key_here
MODEL=gemini-1.5-flash

```


3. รันระบบผ่าน ADK Web:
```bash
adk web workflow_agents

```



---

## 📁 โครงสร้างไฟล์ในโปรเจกต์

* `agent.py`: โค้ดหลักที่บรรจุ Agent และ Logic ทั้งหมด
* `.env`: ไฟล์เก็บความลับ (API Key)
* `court_records/`: โฟลเดอร์เก็บไฟล์รายงาน `.txt` ที่ระบบสร้างขึ้น
* `README.md`: เอกสารอธิบายงาน
